// IndexNowPing removed 2026-09-06.
// Was never imported by layout.tsx; sitemap is pushed directly via the
// Pages Function (/functions/sitemap.xml.js). Re-add here + import from
// layout.tsx if we ever need client-side per-visit pings.
